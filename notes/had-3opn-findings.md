# Verification Report: Browser Data Loading (had-3opn)

## Executive Summary

**Critical Finding**: `web/index.html` has real data loading **disabled**. The flag `USE_REAL_DATA = false` (lines 2384, 2430) forces the dashboard to use mock data regardless of Parquet file availability.

## Files Analyzed

### 1. `web/index.html` - Main Dashboard
**Status**: ⚠️ NOT WIRED TO LOAD REAL DATA

**Issues Found**:
- Line 2384: `const USE_REAL_DATA = false;` - **Disables real data loading**
- Line 2430: `const USE_REAL_DATA = false;` - **Disables real data loading in refresh loop**

**What DOES Work**:
- ✅ DuckDB-WASM loading code is correct (lines 2200-2218)
- ✅ Parquet URL is correct: `/current_snapshot.parquet` (line 2145) - follows ADR-7 same-origin
- ✅ httpfs extension loading is implemented (lines 2135-2138)
- ✅ Error handling and fallback to mock data is in place (lines 2057-2105)
- ✅ Data staleness tracking from `fetched_at` is implemented (lines 2164-2172)
- ✅ Schema conversion from DuckDB to internal format is correct (lines 2220-2259)

**What is NOT Working**:
- ❌ Real data loading path is never executed because `USE_REAL_DATA = false`
- ❌ Dashboard will ONLY show mock data, regardless of `/current_snapshot.parquet` availability

**Fix Required**:
```javascript
// Lines 2384 and 2430 need to change from:
const USE_REAL_DATA = false;
// To:
const USE_REAL_DATA = true;
```

### 2. `test_output/conformance/conformance_test.html` - Phase 3 Test
**Status**: ⏳ EXISTS BUT NEVER BROWSER-TESTED

**File Details**:
- Generated by: `pipeline/run_conformance_test.py`
- Location: `test_output/conformance/conformance_test.html`
- Parquet file: `test_output/conformance/conformance_test.parquet`
- Size: 16KB HTML, 8KB Parquet

**Test Coverage** (8 tests):
1. ✅ Load DuckDB-WASM from CDN
2. ✅ Register httpfs extension for HTTP range requests
3. ✅ Load Parquet file via httpfs (same-directory file)
4. ✅ Verify schema matches Data Models specification (23 columns)
5. ✅ Verify row count (expects 4 test rows)
6. ✅ Test NULL handling for unmatched CPUs (listing_id='conformance-3')
7. ✅ Test complex types - disks list of structs (listing_id='conformance-2')
8. ✅ Test derived metrics calculation (price_effective_monthly, price_per_gb_ram)

**Critical Point**: This test **exists and is complete** but there is **no evidence it has ever been opened in a real browser and passed**.

### 3. Placeholder Values Scan
**Status**: ✅ CLEAN - NO PLACEHOLDERS FOUND

Searched for: `example.com`, `TODO`, `FIXME`, `Replace with`
- ✅ No placeholder values found in `web/*.html`, `web/*.js`, `web/*.ts`

## What Needs Verification

### Immediate (Before Closing This Bead)

1. **Test Conformance Test in Real Browser**
   - Serve `test_output/conformance/` directory via HTTP
   - Open `conformance_test.html` in a browser
   - Click "Run Conformance Tests" button
   - Verify all 8 tests pass

2. **Set Up HTTP Server for Testing**
   ```bash
   cd /home/coding/hetzner-auction-dashboard
   python3 -m http.server 8080
   # Then open: http://localhost:8080/test_output/conformance/conformance_test.html
   ```

### After had-3080 Deploy (Future Work)

1. **Enable Real Data Loading**
   - Change both `USE_REAL_DATA = false` to `true` in `web/index.html`
   - Deploy to Cloudflare Pages
   - Verify dashboard loads `/current_snapshot.parquet` successfully

2. **Verify Live Site**
   - Open `https://hetzner-auction-dashboard.pages.dev`
   - Check browser console for successful DuckDB-WASM initialization
   - Verify data staleness indicator shows actual timestamp
   - Confirm listings display real auction data, not mock data

## Risk Assessment

**Severity**: HIGH - Dashboard is **non-functional for production use** despite having all the correct code infrastructure.

**Impact**:
- Users will only see mock data, never real auction listings
- Phase 5 deployment will appear successful but will not actually serve data
- All downstream decisions based on dashboard data will be using mock samples

**Mitigation**:
1. Flip `USE_REAL_DATA = true` flag before production deployment
2. Add CI checks to prevent `USE_REAL_DATA = false` in production builds
3. Browser-test the conformance test HTML to verify DuckDB-WASM actually works

## Recommendations

1. **Immediate**: Run the conformance test in a browser to prove DuckDB-WASM + Parquet actually works
2. **Before Deploy**: Add a pre-deploy check that `USE_REAL_DATA = true` in production builds
3. **Documentation**: Update deployment docs to explicitly mention the `USE_REAL_DATA` flag
4. **Monitoring**: Add error tracking to detect if real data loading fails in production

---

**Generated**: 2026-08-06
**Bead**: had-3opn
